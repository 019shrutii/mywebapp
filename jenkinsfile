pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                bat 'docker build -t mywebapp .'
            }
        }

        stage('Push') {
            steps {
                bat 'docker push YOUR_USERNAME/mywebapp:latest'
            }
        }

    }
}